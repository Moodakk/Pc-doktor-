[project]
name = "dental-archive-manager"
version = "0.1.1"
description = "Offline dental imaging archive organizer with verified copy and safe cleanup workflow"
requires-python = ">=3.11"
dependencies = [
  "pydicom==3.0.1",
  "Pillow==12.2.0",
  "Send2Trash==2.1.0",
]

[tool.unittest]
start-directory = "tests"
